# func_8005F948

## Boundary
VRAM 0x8005F948-0x8005F9CC, size 132 bytes; the return jump's delay slot is
empty. Single caller targets the entry. Boundary as assigned is correct.

## Callers / callees
1 caller. Calls func_8003B250 (object pointer, zero) and func_8003B184 (two
object pointers and a floating-point value).

## Globals
D_800AD090, read as a 32-bit float.

## Regional addresses
jp 0x800604B0 (rom 0x610B0), eu 0x80064E2C (rom 0x65A2C), lrg_rev1 shares the
USA address. In jp the callees are func_8003BBD0 / func_8003BB04 and the
constant is D_800B02D8; in eu they are func_8003FB90 / func_8003FAC4 and
D_800B5CF8. All three regional bodies are structurally identical to USA.

## Behavior
Resets a sub-object embedded in the structure it is given. It clears four
consecutive 32-bit fields at offsets 0x54 through 0x60, copies the handle held
at offset 0x2C into offset 0x34, and clears three floats at offsets 0x38, 0x3C
and 0x40. If that handle is non-null, a preparation routine is called on the
sub-object starting at offset 0x34 with a zero selector. The sub-object is then
bound to the region at offset 0x64 with a zero floating-point parameter, and
finally the float immediately after the handle (offset 0x38) is set from the
module constant.

## Status
exact (USA, jp, eu, lrg_rev1).
