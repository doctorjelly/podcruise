# func_80052A08 — screen channel layout

## Verified facts

- The USA body spans VRAM `0x80052A08` through `0x80052E43`, ROM `0x53608`,
  1084 bytes. Its return is at `0x80052E3C`; the delay slot at `0x80052E40`
  is `nop`.
- The USA function calls `func_80051FF4`, `func_8000A920`,
  `func_800862D8`, `func_8008635C`, and `func_80086730`. Its callers are
  `func_80052E44`, `func_800530CC`, `func_8005337C`, `func_8005C36C`, and
  `func_8005CBD8`. It reads/writes `D_800A52B8`, `D_800A52C8`, and
  `D_800A52CC`.
- Cross-version structural matches place the equivalent at Japan VRAM
  `0x80053678` (ROM `0x054278`) and Europe VRAM `0x800576B8` (ROM
  `0x0582B8`).

## Behavior

The routine updates a screen state when requested, rejects inactive or
already-final states, and configures the display channel rectangles and
scales for single-player, two-player, and multi-player layouts. It handles
the special bit flags in the screen object, performs the required graphics
helper calls, and records the selected state. The original uses fixed
rectangle coordinates, split-screen offsets, and floating-point scale values.

## Status

Behavior recovered and independently expressed in C. IDO 5.3 emits the same
1084-byte symbol size, but comparison against the USA ROM differs at byte
`0x31E` (15 byte differences across 10 instruction words in the localized tail).
The remaining differences are floating-point register coloring/conversion
scheduling; this is an inference from the localized instruction comparison,
not a claim of exact source recovery.
