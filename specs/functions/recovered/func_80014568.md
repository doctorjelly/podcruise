# func_80014568 — matching blocker

- USA ROM `0x15168..0x15897` is a 1,840-byte body through its return delay
  slot. The reviewed C candidate has the correct linked symbol size and
  address, with 13 differing instruction words, first at byte `0x311`.
- Differences repeat around accesses to `D_800D6938`: the ROM materializes
  its address in `$a2` where the compiler uses `$v1`. Two pointer-local
  variants changed the stack frame and prologue, so neither was retained.
  Keep the USA split as assembly until register allocation is resolved.
