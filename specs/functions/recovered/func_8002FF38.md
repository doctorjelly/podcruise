# func_8002FF38 — matching blocker

- USA ROM `0x30B38..0x30D2F` is a 504-byte body. The current C candidate has
  the correct linked symbol size and address, with 38 differing instruction
  words, first at byte `0x64`.
- A targeted offset/entry-pointer form shortened the symbol to 496 bytes and
  broadened the mismatch to 121 words, so it was discarded. The allocator
  interactions among `dest`, `size`, and the exhausted loop index remain
  unresolved. Keep the USA split as assembly.
