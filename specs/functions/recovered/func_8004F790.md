# func_8004F790

## Verified facts

- The USA body spans VRAM `0x8004F790` through `0x8004FE2F`, ROM `0x50390`,
  1696 bytes. `jr ra` is at `0x8004FE28`; its delay slot restores the
  208-byte stack frame. `func_8004FE30` begins next.
- Its only direct caller is `func_8004F358`. Its random-number, profile-copy,
  profile-adjustment, and preferred-slot helpers and all referenced global
  tables are recorded in the matching unit.
- Japan and Europe equivalents begin at `0x800503FC` and `0x80054438`.

## Behavior

When enabled, the routine fills a global participant-slot table for the
requested local and computer entries. It chooses style indices subject to the
holder's constraints, assigns slot kinds and order, copies or derives each
profile, selects a preferred slot, and marks only that entry. A disabled call
simply returns the existing table.

## Status

Behavior recovered from the USA binary and independently expressed in C; not
byte-matching. IDO 5.3 `-O2` emits the exact 1696-byte length and first differs
at byte `0x9`; 267 instruction words differ across 39 blocks. Regional binaries
were used only for structural and relocation confirmation.
