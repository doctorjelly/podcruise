# func_80091354 — matching blocker

- USA ROM `0x91F54..0x92227` is a 724-byte body through its return delay
  slot; following padding is not part of the function. The current C candidate
  has the correct linked symbol size and address, with 31 differing instruction
  words, first at byte `0xA1`.
- Reordering local declarations left the mismatch unchanged. Reordering the
  count/mode logic shortened the symbol to 720 bytes and broadened the
  mismatch, so both variants were discarded. Residual stack-local/codegen
  shape needs new evidence; keep the USA split as assembly.
