# func_800141EC — matching blocker

- USA ROM `0x14DEC..0x15167` is one 892-byte function. The disassembler's
  `func_800141F8` label is an interior split: its live `$v0`/`$v1` come from
  the 12-byte prefix at `0x800141EC`.
- The reviewed C candidate has the correct linked symbol size and address,
  but differs from the USA ROM in 10 instruction words, first at byte `0x261`.
- A local `ch` copy changed the prologue and layout; a local declaration
  reorder did not improve the 10-word mismatch. Both were discarded. The
  remaining differences are localized register allocation in the
  character/font-boundary logic. Keep the USA split as assembly.
