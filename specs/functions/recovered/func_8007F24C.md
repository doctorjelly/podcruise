# func_8007F24C recovery blocker

The existing behavior-recovered C compiles to the correct 484-byte USA body but
differs in 68 words (first differing byte `0x1D`). Two bounded source-shape
variants were tested: swapping the two floating locals had no effect; spelling
the first comparison as the equivalent `vector_dot > matrix_dot` reduced the
diff to 67 words but was not exact. No source edits retained.
