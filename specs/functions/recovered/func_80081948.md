# func_80081948

## Boundary
VRAM 0x80081948-0x800819A4 (92 bytes), ROM 0x82548; return delay slot empty.
Six direct call sites, all targeting this entry with four pointer arguments.

## Callers / callees
Callers: 6 sites. Callees: func_800156DC, func_80015328, func_80017918.

## Globals
None.

## Regional addresses
jp 0x800825E8 (ROM 0x831E8); eu 0x80086E28 (ROM 0x87A28); lrg_rev1 as USA.

## Behavior
Builds a transform block at a caller-supplied destination from three inputs.
The destination's leading block is first initialised from the third argument
(a source block of the same kind). The destination's sub-block at byte offset
0x30 is then combined with the second argument, the sub-block acting as both
target and first operand. Finally the destination is positioned using the three
consecutive single-precision components read from the fourth argument, with the
destination itself also supplied as a trailing parameter of that call.

## Status
exact in USA, and byte-identical for Japan, Europe and lrg_rev1.
