# func_800419EC — matching blocker

- USA ROM `0x425EC..0x42913` is an 808-byte body through the return delay
  slot. The current linked C symbol is 800 bytes, starts differing at byte
  `0x7`, and differs in 196 comparable instruction words.
- The C compiler allocates a callee-saved floating-point register and changes
  the frame/epilogue; the ROM uses a caller-saved temporary. A float-zero
  spelling variant grew the symbol to 804 bytes but did not repair the
  prologue, so it was discarded. This needs source/layout recovery rather
  than register-only tweaking. Keep the USA split as assembly.
