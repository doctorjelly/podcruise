# func_80048A50 — matching blocker

- USA ROM `0x49650..0x49B73` is a 1,316-byte body through its return delay
  slot. The reviewed C candidate has the correct linked symbol size and
  address, with 17 differing instruction words, first at byte `0x94`.
- Reordering the `scale` and `i` declarations did not improve the comparison.
  Hoisting `i = 0` broadened it to 302 differing words. Both variants were
  discarded. The residual mismatch is localized to scheduling around loop
  initialization; keep the USA split as assembly.
