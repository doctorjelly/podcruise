# func_8005B708

## Boundary
VRAM 0x8005B708-0x8005B764 (92 bytes), ROM 0x5C308. Ends with a return whose
delay slot is empty. One direct caller; entry confirmed as the frame setup.

## Callers / callees
Callers: 1. Callees: func_8003F800, func_80008B14, func_80053300, func_8005C36C.

## Globals
None.

## Regional addresses
jp 0x8005C2B4 (ROM 0x5CEB4); eu 0x80060B84 (ROM 0x61784); lrg_rev1 same as USA.

## Behavior
Takes no arguments and returns nothing. It first looks up a resource by a fixed
four-character tag identifier, passing a zero for the lookup's second selector,
and keeps the returned handle. It then requests a fixed presentation effect:
effect number 143, mode 6, with two fixed fractional magnitudes (one quarter and
one fifth) and an enable flag of one. Finally it hands the previously obtained
handle to two further routines in turn, the first performing the main action on
it and the second a follow-up on the same handle.

## Status
behavior. The compiled body is instruction-for-instruction identical to the
original; only the stack-frame size immediate differs (56 in the original, 40
from this source), i.e. the original reserved 16 further bytes of unreferenced
local space that no plain-C shape reproduced within budget. Optimisation level
and prototype/no-prototype spellings do not affect it.
