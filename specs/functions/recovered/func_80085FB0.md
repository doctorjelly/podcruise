# func_80085FB0 — matching blocker

- USA ROM `0x86BB0..0x86D77` is a 456-byte body. The current linked C symbol
  is 464 bytes, differs from the first instruction (`0x3` byte offset), and
  differs in 108 comparable instruction words.
- The compiler emits a 0x70-byte frame rather than the ROM's 0x68-byte frame.
  Two local-spelling variants did not change that result and were discarded.
  Revisit control flow or local lifetimes rather than register-only changes;
  keep the USA split as assembly.
