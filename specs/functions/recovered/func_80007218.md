# `func_80007218` recovery note

The linked USA C object is the correct size (396 bytes) but differs from the
canonical ROM in two words: at `+0x04`, the ROM has `lui v1,0x800b` while C has
`lui a0,2`, and at `+0x08` the two instructions are exchanged.

Source-line grouping pilot (all retained edits reverted; none exact):

| variant | result |
|---|---|
| bound assignment joined with `for` initializer | 4 differing words, first `0x05` |
| loop header joined with first store | 2 differing words, first `0x05` |
| both groupings | 4 differing words, first `0x05` |

The remaining mismatch is a register allocation/scheduling difference under
these grouping variants.
