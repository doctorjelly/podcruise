# `func_80050C00` USA declaration-order audit

The behavior-recovered C compiles to the canonical 904 bytes, but differs in
121 words, first byte `0x21`; the parent verified the restored baseline. Its
frame size agrees with the ROM at `0x78`. Several lower locals are displaced
by four bytes: the `start` array is at ROM/C `sp+0x20/0x24`, `spanAC` at
`0x48/0x4C`, `spanCB` at `0x3C/0x40`, and the `half` spill at `0x38/0x3C`.
The `spanAB` and `pushed` arrays already agree at `0x64` and `0x58`.

A bounded sweep moved only the existing `start[3]` declaration through the
local declaration order. Its best placement, after `span`, reduced the word
difference from 121 to 120 but did not match. All variants were restored;
do not repeat this single-local sweep without a new stack-layout hypothesis.
