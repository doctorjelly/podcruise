# func_8005EC18

## Verified facts

- The canonical USA body spans VRAM `0x8005EC18` through `0x8005ED6F`, ROM
  `0x5F818`, for 344 bytes. Its `jr ra` at `0x8005ED68` restores the 72-byte
  frame in the delay slot; `func_8005ED70` begins immediately afterward.
- `func_8005EF08` is the one direct recovered caller. Its context argument is
  not read by this routine.
- The routine calls `func_80030298`, `func_8003FC94`, `func_800305E8`,
  `func_80018324`, `func_800181BC`, and `func_80065804`.
- It addresses the 40-entry arrays `D_8011B8A0`, `D_8011B940`, and
  `D_8011C700`, then publishes `D_8011B880` through `D_8011AC9C`.
- The available cross-version map does not uniquely locate the regional body,
  so no Japan or Europe compiler-match claim is made.

## Behavior

Register the fixed `Smok` tag with two setup helpers, then initialize forty
slots. For each slot, request resource `0x93`. When present, initialize the
corresponding item with tag `0xD065`, attach a word copied from the resource,
publish the item in the pointer table, and configure it with the fixed
parameters 2, -4, 16, and 3. When absent, clear that table entry. Finally,
initialize the root record with tag `0x5064`, attach the forty-entry pointer
table, publish the root, and submit the table to the final helper.

## Status

Byte-matching C in canonical USA. Exact linked IDO 5.3 `-O2` comparison
reproduces all 344 bytes. Retaining the behavior-neutral exhausted-loop
invariant expression selects the ROM's saved-register allocation; no regional
byte match is claimed.
