# func_80035BF0

## Verified facts

- The USA body spans VRAM `0x80035BF0` through `0x80036093`, ROM `0x367F0`,
  1188 bytes. `jr ra` is at `0x8003608C`, followed by a nop delay slot;
  `func_80036094` begins next.
- Direct callers are `func_80036F98`, `func_800374C4`, `func_80038950`, and
  `func_80038C48`. Its only callee is the byte comparator `func_80081530`.
- It reads the default state at `D_800A3D44`, writes through the display-list
  cursor `D_80112C90`, and maintains cached render fields at `D_80112DE4`
  through `D_80112E0E`.
- JP and EU equivalents begin at `0x80036650` and `0x8003A560`.

## Behavior

The routine compares a requested render-state record with cached state and
emits only the required display-list commands, unless forced. It covers cycle
mode, combiner muxes, geometry/render bits, and four packed color fields.

## Status

Behavior recovered, not byte-matching. IDO 5.3 `-O2` emits the exact 1188-byte
length but first differs at byte `0x81`; the remaining differences span 83
instruction words, so this is not a localized scheduling miss.
