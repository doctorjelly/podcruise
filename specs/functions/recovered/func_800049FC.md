# func_800049FC — matching blocker

- USA ROM `0x55FC..0x59FB` is a 1,024-byte body. The current C candidate has
  the correct linked symbol size and address, with 33 differing instruction
  words, first at byte `0x93`.
- A byte-offset child traversal worsened the comparison to 43 words; a
  `count * 4` indexed traversal grew the symbol to 1,044 bytes. Both were
  discarded. Further work needs evidence beyond these loop spellings; keep
  the USA split as assembly.
