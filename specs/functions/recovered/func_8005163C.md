# func_8005163C — matching blocker

- USA ROM `0x5223C..0x523D7` is a 412-byte body through the return delay slot.
  The current linked C symbol is 404 bytes, starts differing at byte `0x18`,
  and differs in 93 comparable instruction words (parent verifier; the last
  two target words have no compiled counterparts).
- Nested `do`/`while` and explicit `for`/break forms did not improve the
  comparison and were discarded. The mismatch begins in the prologue and
  saved-register layout, so source recovery needs new compiler/layout evidence
  rather than further blind loop spelling. Keep the USA split as assembly.
