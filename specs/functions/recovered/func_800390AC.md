# func_800390AC — empty routine

## Boundary

- USA VRAM range `0x800390AC` .. `0x800390B3` inclusive, **8 bytes**, ROM
  `0x39CAC`, translation unit `main_18B28.s`. The assignment listed 16, twice
  the true length.
- The whole body is a return plus an empty delay slot; both instructions were
  checked, so this is genuinely a two-instruction function and not a return
  spliced onto a neighbour.
- Two direct call sites target `0x800390AC` exactly. The three padding words
  that follow at `0x800390B4`..`0x800390BF` are alignment for the next function
  at `0x800390C0`, not part of the body: the assembled function is 8 bytes and
  verifies at that length.
- It sits in a run of similar stubs (`0x80039090`, `0x800390A4`), which is
  consistent with a group of routines that were compiled out or never filled in.

## Callers

Two direct call sites, both in `main_840F0.s`, inside the routines at
`0x80084F1C` and `0x800852E8`. Neither site sets up any argument register for
the call — the instruction in each delay slot is a store belonging to the
preceding display-list work — and neither uses a return value. The signature is
`void (void)`.

## Callees

None (leaf).

## Globals and access widths

None. The routine touches no memory and no registers.

## Regional addresses

| Version | VRAM | ROM | Size |
| --- | --- | --- | --- |
| USA | `0x800390AC` | `0x39CAC` | 8 |
| Japan | `0x80039B0C` | `0x3A70C` | 8 |
| Europe | `0x8003DA1C` | `0x3E61C` | 8 |
| lrg_rev1 | `0x800390AC` | `0x39CAC` | 8 |

Identical in all four; there are no symbols to relocate.

## Recovered behaviour

The routine does nothing and returns. It takes no arguments and produces no
result; calling it has no observable effect.

## Status

Exact. Byte-identical in USA, Japan, Europe and lrg_rev1, at 8 bytes.
