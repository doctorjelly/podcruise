# `func_80082624`

## Verified facts

The canonical USA body occupies VRAM `0x80082624` through `0x800827B3`, ROM
`0x083224` through `0x0833B3`, for 400 bytes. The return is at `0x800827AC`;
its delay slot restores the 80-byte stack frame. Three padding instructions
follow before the next established entry at `0x800827C0`.

Five direct USA calls occur at `0x8000B6D8`, `0x8000B788`, `0x8000B7B8`,
`0x8000C188`, and `0x800437F0`. The body directly calls `func_800153C0`,
`func_800155C0`, `func_80014D4C`, `func_80014F54`, `func_800174B8`, and
`func_80017580`; it contains no indirect call.

The addressed local constants are a 32-bit `0.0001` threshold at
`0x800ADCD8`, a 64-bit `0.0001` at `0x800ADCE0`, and a 64-bit `-0.0001` at
`0x800ADCE8`. An eight-byte adjacent literal pair starts the modeled unit pool
at `0x800ADCD0` and places those constants at their canonical addresses.

Japan carries the same-size body at VRAM `0x800832C4`, ROM `0x083EC4`, and
Europe at VRAM `0x80087B04`, ROM `0x088704`. Their returns and frame-restore
delay slots have the same relative offsets. The USA LRG revision retains the
USA address and bytes.

## Behavioral specification

Copy the starting point into the output record and calculate the vector from
that point to the destination. If its magnitude is meaningful, normalize it,
derive the output's pitch from the vertical component, retain the caller's roll,
and derive the remaining angle from the horizontal components. A nearly zero
second component uses explicit positive, negative, and centered cases instead
of the general angle helper. Build the output transform from the resulting
five-value record. When both points are effectively coincident, build a pure
translation transform at the starting point instead.

Calling the result an orientation transform is an inference from the vector
normalization, angle helpers, and matrix-building calls. The comparisons,
constants, field positions, and call sequence are directly recovered facts.

## Status

Byte-matching C. IDO 5.3 with the recorded `-O2` profile reproduces all 400
text bytes exactly for USA retail and the USA LRG revision. Existing targeted
comparisons reproduce the relocated Japan and Europe bodies exactly as well.
