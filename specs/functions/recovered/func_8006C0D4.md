# func_8006C0D4

## Verified facts

- The USA body spans VRAM `0x8006C0D4` through `0x8006C6C7`, ROM `0x6CCD4`,
  1524 bytes. `jr ra` is at `0x8006C6C0` with a nop delay slot. The two nops at
  `0x8006C6C8` and `0x8006C6CC` are alignment padding; `func_8006C6D0` begins
  next.
- No direct `jal` targets the routine. Its address occurs once in the USA data
  table at ROM `0xA2BFC`, establishing indirect dispatch. Its motion, contact,
  collision, timer, and state helpers and globals are in the matching unit.
- Japan and Europe equivalents begin at `0x8006CC44` and `0x800715B4`.

## Behavior

This is a vehicle motion update. It selects airborne or ground contact
resolution, updates velocity and signed speed, advances collision flags and
timers, applies the appropriate contact or free-motion corrections, updates
linked triggers, and restores two temporary subsystem settings before return.

## Status

Behavior recovered from the USA binary and independently expressed in C; not
byte-matching. IDO 5.3 `-O2` emits 1516 bytes for the 1524-byte target and is
identical through byte `0x1F3`; 53 instruction words differ across 11 blocks.
Three evidence-driven expression/lifetime variants did not improve the match.
