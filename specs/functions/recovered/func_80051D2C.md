# func_80051D2C — matching blocker

- USA ROM `0x5292C..0x52BF3` is a 712-byte body. The current C candidate has
  the correct linked symbol size and address, with 36 differing instruction
  words, first at byte `0x61`.
- Preselecting the scrolling-table pointer shortened the symbol to 596 bytes;
  reversing entry/value loads increased the mismatch to 41 words; same-line
  grouping left it at 36. All were discarded. The remaining mismatch is a
  shared scrolling-table code-generation difference; keep assembly in USA.
