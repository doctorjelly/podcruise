# func_8005EA70

## Boundary
VRAM 0x8005EA70-0x8005EAC0, ROM 0x5F670, size 80 bytes. Ends with a return whose
delay slot carries the frame teardown. One direct caller; boundary confirmed.

## Callers / callees
1 caller. Calls func_80030298, func_8003FC94 and func_8007E0EC.

## Globals
D_8011AC90, written as a word with the result of the third call.

## Regional addresses
jp 0x8005F5D8 / ROM 0x0601D8 (callees func_80030CF8, func_80040624,
func_8007ED8C; global D_8011DF00);
eu 0x80063F54 / ROM 0x064B54 (callees func_80033858, func_80044634,
func_800835CC; global D_80123930);
lrg_rev1 shares the USA addresses.

## Behavior
Takes a pointer to a large record. It announces a fixed four-character tag
(the ASCII letters "Trig") together with the constant 30 to one bookkeeping
routine, then passes the same tag alone to a second bookkeeping routine. It
then reads three word fields from the record - at byte offsets 0x1AC, 0x1C0 and
0x30 - passes them to a third routine, and stores that routine's result in a
single global word. Nothing is returned to the caller.

## Status
exact (USA, Japan, Europe, lrg_rev1).
