# func_80016F0C

Three bounded source-shape checks were attempted against the existing
behavior-recovered C. The baseline and best variant both compile to 816 bytes,
but the best result remains 60 differing words (first difference `0xB9`).
Declaration-order changes worsened the result to 68 and 74 differing words.
No source change is retained; the remaining mismatch is a register/scheduling
problem not resolved by these evidence-driven variants.
