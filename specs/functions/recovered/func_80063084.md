# func_80063084

## Verified facts

- The USA function spans VRAM `0x80063084` through `0x80063343`, ROM
  `0x63C84`, 704 bytes. Its `jr ra` is at `0x8006333C`; the delay slot at
  `0x80063340` is `nop`.
- Four callers establish six arguments: two integers, two vector pointers, and
  two floats. The lookup result contains a state at `0x08`, node/list pointers
  at `0x30`/`0x34`, vectors at `0x44`, `0x50`, and `0x5C`, floats at `0x68`,
  `0x6C`, `0x78`, and `0x94`.
- The routine calls `func_8003F800`, vector/matrix helpers, an object flag
  helper, `func_80082BE0`, three node update helpers, and `func_80060DE4`.
- The twelve-way jump table starts at `D_800AD350`. Its case constants follow
  at `D_800AD380` through `D_800AD3B8`.
- Japan maps uniquely to `0x80063BF4` / ROM `0x647F4`; Europe maps uniquely to
  `0x80068564` / ROM `0x69164`.

## Behavior

Look up an object by tag and index, returning if it is absent. Copy the two
supplied vectors into three object slots in first/second/first order, then store
the two scalar parameters. If its render node has a live handle, update that
handle with a matrix built from the first vector and first scalar. Select a
scale parameter from the object's
twelve-state selector; the first state can also randomize a secondary value,
and the second state varies with the caller's mode. Apply position, scale,
angle, and secondary-value updates to the object's nodes, then dispatch the
caller's mode to the object runtime.

## Status

Behavior recovered, not byte-matching. A linked IDO 5.3 `-O2` comparison that
includes the generated jump table and constant pool emits 660 text bytes rather
than 704; the first difference is the stack-frame size. The remaining text and
read-only-data differences are broad local-layout, case-order, and common-block
scheduling changes, so no exact claim is made.
