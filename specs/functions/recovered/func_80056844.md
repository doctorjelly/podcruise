# func_80056844

## Verified facts

- The USA body spans VRAM `0x80056844` through `0x800573E7`, ROM `0x57444`,
  2,980 bytes. `jr ra` is at `0x800573E0`, its delay slot is `nop`, and
  `func_800573E8` begins next.
- Its only direct caller is `func_800573E8`, at three mode-selection sites.
  Its sprite-layout, game-mode, progress, and fade helpers and referenced
  globals are recorded in the matching unit.
- Japan and Europe equivalents begin at `0x80057408` and `0x8005B524`.

## Behavior

Configure the reusable HUD background sprites for a requested bitmask. It
switches between wide and compact panel layouts, places shared frame pieces,
shows either fixed separators or a progress-dependent vertical fill, and
enables a shaded indicator according to the global fade value.

## Status

Behavior recovered from the USA binary and independently expressed in C; not
byte-matching. IDO 5.3 `-O2` emits 2,472 bytes for the 2,980-byte target and
first differs at byte `0x2`.
