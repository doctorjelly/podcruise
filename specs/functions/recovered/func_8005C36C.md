# func_8005C36C

## Verified facts

- The USA body spans VRAM `0x8005C36C` through `0x8005CBD7`, ROM `0x5CF6C`,
  2156 bytes. `jr ra` is at `0x8005CBD0`; its delay slot restores the
  280-byte stack frame. `func_8005CBD8` begins next.
- Its direct caller is `func_8005B708`. The audio, interface, ranking, session,
  and object helpers and globals are recorded in its matching unit.
- The Japan equivalent begins at `0x8005CF18`. No unique Europe equivalent was
  established, so Europe is not used as evidence for this recovery.

## Behavior

The routine advances post-race session and interface state. It selects result
audio, updates transitions and fades, controls a blinking status, constructs a
ranking table, clears HUD state, and invokes the appropriate session follow-up
helpers.

## Status

Behavior recovered from the USA binary and independently expressed in C; not
byte-matching. IDO 5.3 `-O2` emits the exact 2156-byte function symbol (the
object section has four additional alignment bytes) and first differs at byte
`0x327`; 91 instruction words differ across 59 blocks.
