# func_800678A8 — matching blocker

- USA ROM `0x684A8..0x686B3` is a 524-byte body. The current C candidate has
  the correct linked symbol size and address, with 38 differing instruction
  words, first at byte `0x22`.
- Moving address-taken `count` before `cp` or after `sp48` in the local
  declaration order worsened the mismatch to 44 or 47 words. Both variants
  were discarded. Keep the USA split as assembly pending new stack-layout
  evidence.
