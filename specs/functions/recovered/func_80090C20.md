# func_80090C20 blocker

Baseline and three declaration-placement variants all remain 848/848 bytes,
69 differing words, first difference byte `0x03`. Moving `attempt` after the
buffers, after the pointer/code locals, or between `high` and `low` did not
shift the compiler frame (`high`/`low` stayed at the C-layout offsets), so the
requested frame-placement hypothesis was not sufficient. No source change
retained; further work likely requires pointer-lifetime/source-shape analysis.
