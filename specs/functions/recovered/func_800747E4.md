# `func_800747E4` USA compiler audit

The current C candidate compiles to 796/804 bytes, with 181 differing
comparable words and two missing words; the first byte mismatch is `0x2B`.
Its 18 direct helper calls remain in canonical order. The ROM materializes a
shared `1.0f` before the direction-zero early return and reuses it for the
positive direction and later clamp. Current C materializes the clamp value
after that return.

Two bounded source variants introduced a typed `f32 one`: assigning it before
direction selection and reusing it for positive direction/clamp, or assigning
it immediately after direction selection and reusing it for the clamp. Both
left the comparison unchanged at 796/804 bytes, 181 differing words, and two
missing words. The source was restored and independently rechecked. Do not
repeat shared-one placement without new FP-register lifetime evidence; the
USA split remains assembly.
