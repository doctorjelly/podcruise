# func_800467F8

## Boundary
USA VRAM 0x800467F8-0x80046870 (120 bytes, 30 instructions). The return's
delay slot restores the stack pointer. One direct caller targets the entry;
no branch from the preceding function reaches into the body. Boundary not
corrected.

## Callers
One direct caller.

## Callees
- func_80005B80, called once with no arguments before the loop.
- func_80030964, called once per non-empty slot with that slot's value.

## Globals
- D_8011A508: a table of 151 words, read one word at a time.
- D_8011A764: the word immediately past that table; it appears only as the
  loop's end address, not as a value.
- D_8011A768: a parallel table of 151 words, written one word at a time.

## Regional addresses
- Japan: function 0x80047008 (ROM 0x047C08); tables at 0x8011D778 and
  0x8011D9D8; the second callee is at 0x800313C4.
- Europe: function 0x8004B198 (ROM 0x04BD98); tables at 0x801231A8 and
  0x80123408; the second callee is at 0x80033F24.
- lrg_rev1: shares the USA addresses.

## Recovered behavior
The routine first performs a one-off preparation step by calling
func_80005B80. It then walks all 151 entries of the first table in order.
Every entry that is non-zero is passed to func_80030964, and the value that
call returns is stored at the matching index of the second table. Zero entries
are skipped and leave the corresponding output slot untouched. The routine
returns nothing.

## Status
exact. USA, Japan, Europe and lrg_rev1 all verified byte-identical.
