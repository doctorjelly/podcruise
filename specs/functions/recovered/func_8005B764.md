# func_8005B764

## Verified facts

- The canonical USA body is VRAM `0x8005B764` through `0x8005B80F`, ROM
  `0x5C364`, 172 bytes. It returns at `0x8005B808` with a `nop` delay slot at
  `0x8005B80C`; the next function begins at `0x8005B810`.
- The only direct caller is at `0x8002F2A8` inside `func_8002F1EC`.
- The function calls `func_80053220(0x200)` and looks up tag `0x4A646765`,
  index zero, through `func_8003F800`. It touches no data globals. The jump
  table at `0x800BCFB4` is compiler-generated read-only data.
- Japan (`0x8005C310`, ROM `0x5CF10`) and Europe (`0x80060BE0`, ROM
  `0x617E0`) have the same normalized structure across all 172 bytes.

## Behavior

The first helper supplies a Boolean-like availability result. The tagged entry
supplies a flag word at offset eight. Flag bit `0x20` forces a zero result.
Otherwise low-nibble values 2, 4, 5, and 6 force zero; every other value returns
the availability result. The tagged lookup is assumed to succeed, matching the
original unconditional field read.

## Status

Behavior recovered, not byte-matching. IDO 5.3 `-O2` emits a 176-byte body
against the 172-byte original and first differs at byte `0x25`: the recovered
shape assigns the flag, availability, and result values to different registers
and needs a final result move. No exact-match claim is made.
