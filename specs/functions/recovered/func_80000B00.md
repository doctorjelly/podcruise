# `func_80000B00` USA compiler audit

The current C source compiles to 580/640 bytes, with 125 differing comparable
words and 15 missing words; the first byte mismatch is `0x03`.

At `80000C44–80000C80`, the ROM caches probe z/x/y in stack slots
`0x4C/0x48/0x44` for the edge-B distance. It refreshes all three inside the
edge-B winner branch at `80000CC4–80000CDC`, then consumes the cached values
for edge C at `80000CE0–80000D04`. The current C directly reloads probe
fields for both distances.

Three bounded ordinary-`f32` source-shape variants were tested:

| variant | compiled/ROM bytes | differing words | missing words |
|---|---:|---:|---:|
| cache before edge B | 576/640 | 122 | 16 |
| direct edge B, cache before edge C | 604/640 | 133 | 9 |
| cache edge B, direct edge C | 580/640 | 125 | 15 |

None was exact. All edits were reverted, and the parent reran the 580/640
baseline comparison. The ROM also spills edge-B `dy` at `sp+0x38` before
squaring; that detail was not separately tested. Do not repeat the three
cache-only variants without new stack or alias evidence.
