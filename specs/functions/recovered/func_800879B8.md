# `func_800879B8` USA compiler audit

The baseline compiles to 744 bytes against the 756-byte ROM body, with 172
differing comparable words, three missing words, and first byte difference
`0x0C`. Three bounded variants (explicit refreshed bound local, volatile
global bound, and reordered rectangle-coordinate calculations) did not match
exactly. The best had 170 differing words at the same size. All source edits
were restored; the remaining differences are broad code-generation and
scheduling mismatches.
