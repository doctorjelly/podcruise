# func_80054048 — matching blocker

- USA ROM `0x54C48..0x553EB` is a 1,956-byte body. The reviewed C candidate
  links at the canonical address with the correct symbol size, but differs in
  20 instruction words, first at byte `0x3`.
- The baseline C frame is 0x80 bytes versus the ROM's 0x78. Changing the
  unused `spare` array from four floats to one reduced the mismatch to 18
  words, first at byte `0x68`; this was independently verified but remains
  nonmatching, so the source was restored. Moving that one-float local through
  the existing declaration order did not improve it. Target stack offsets
  near `0x64` still differ from the compiled `0x70`; keep the USA split as
  assembly.
