# `func_80085AB4` USA compiler audit

The current behavior-recovered C compiles at the canonical address and size
(656/656 bytes), but differs in 111 words, first byte `0x49`. The function
has no direct helper calls. The ROM keeps signed `value >> 1` and
`value >> 11` intermediates before masking for the histogram indices; the
current compiler allocates their disposable registers differently. The ROM's
float accumulation also uses the accumulator as the left operand.

Separate typed shift locals and, independently, spelling the accumulation as
`total += ...` each left the comparison unchanged at 111 words. The combined
form was not tested because neither isolated change helped. The parent
verified the restored baseline; the USA split remains assembly. More
source-order tests need new scheduling evidence.
