# func_800665A4

## Verified facts

- The USA body spans VRAM `0x800665A4` through `0x80066A7F`, ROM `0x671A4`,
  1244 bytes. `jr ra` is at `0x80066A78`; its delay slot restores the
  216-byte stack frame. `func_80066A80` begins next.
- Both direct calls come from `func_80066A80`. The trigger lookup and dispatch
  callees and the comparison constant are recorded in its matching unit.
- Japan and Europe equivalents begin at `0x80067114` and `0x8006BA84`.

## Behavior

The routine walks a linked list of trigger volumes, filtering each one by its
flags and the actor's current mode and state. For eligible entries it builds
the trigger segment, finds the actor's nearest point on it, applies the
height/radius or crossing test appropriate to the trigger kind, and dispatches
the trigger when the actor intersects it.

## Status

Behavior recovered from the USA binary and independently expressed in C; not
byte-matching. IDO 5.3 `-O2` emits 1232 bytes for the 1244-byte target and first
differs at byte `0x2F`; 295 instruction words differ across six blocks.
Regional binaries were used only for structural and relocation confirmation.
