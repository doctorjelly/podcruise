# func_800963B4

## Boundary

- USA VRAM `0x800963B4` through `0x800963CB`, ROM `0x96FB4`, 24 bytes.
- The return at `0x800963C4` has `move v0, zero` in its delay slot; that
  instruction establishes both the end of the body and its zero result.
- The preceding routine returns at `0x800963AC`, while the next prologue starts
  at `0x800963CC`. No branch crosses either boundary.

## References

The direct call at `0x8008DD08` passes a destination, mode one, and a pointer
to the containing element. `func_8008D870` also registers this address as a
three-argument callback. The function is a leaf and touches no globals.

The structurally equivalent entries are `0x800970A4` in Japan and `0x8009B904`
in Europe. LRG retains the USA address and body. Non-USA builds were used only
to confirm structure.

## Behavior

When the mode equals one, stores the third argument through the destination
pointer. Other mode values leave the destination unchanged. It always returns
zero, including after a store.

## Status

Byte-matching C. The independent source reproduces all 24 bytes with IDO 5.3
and the accepted `ido53_o2` profile for USA retail and the LRG USA revision.
