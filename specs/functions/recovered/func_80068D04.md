# func_80068D04 — matching blocker

- USA ROM `0x69904..0x69C7B` is an 888-byte body. The reviewed C candidate
  links at the canonical address and has the correct symbol size, but differs
  in 24 instruction words, first at byte `0x91`.
- Differences span dot-product register allocation and the final factor-limit
  section. Moving the `limit` declaration earlier left the same comparison.
  That variant was discarded; keep the USA split as assembly.
