# `func_80076B34` USA semantic correction

The reviewed C remains behavior-only. Its original linked USA comparison was
1300/1312 bytes with 184 differing comparable words, three missing words,
and first difference at `+0x125`; the 21 direct call targets and order agree.

At ROM `0x80076BF8`, `$s5` is set to `sp+0x14C`. The conditional
`func_800155EC` call at `0x80076C8C` receives `$s5` for both its first and
second arguments. The old C incorrectly passed `&sp140` as the destination;
the following unconditional helper overwrites that different temporary.
The source now passes `&sp14C` for both arguments, matching the ROM behavior.

The parent linked-object verifier reports 1300/1312 bytes, 183 differing
words, three missing words, first at `+0x12C`. This is a verified behavioral
correction, not a byte match. The USA split remains assembly; further work
needs new evidence for the remaining broad allocation and 12-byte deficit.
