# func_80011928 — clear one word-sized flag

## Boundary
- USA VRAM `0x80011928` .. `0x80011933` inclusive; ROM `0x12528`; size 12 bytes
  (3 instructions).
- Terminates with a return whose delay slot performs the store of zero.
- The previous function ends with its own return one instruction earlier and
  writes a non-zero value to the same global; the single direct call site
  targets `0x80011928`, not that earlier entry. Boundary confirmed as assigned.

## Callers
One direct call site, in the routine beginning near `0x8005D328`, reached only
when a preceding counter comparison fails; the call is followed immediately by
an unconditional branch to that routine's epilogue.

## Callees
None. Leaf function, no stack frame.

## Globals and access widths
| symbol | width | access |
| --- | --- | --- |
| `D_8009B810` | 32-bit word | one store of zero |

## Regional addresses
| build | entry | ROM | global |
| --- | --- | --- | --- |
| USA | `0x80011928` | `0x12528` | `D_8009B810` |
| Japan | `0x80011928` | `0x12528` | `D_8009C500` |
| Europe | `0x80011DB8` | `0x129B8` | `D_800A0D60` |
| `lrg_rev1` | same as USA | same as USA | same as USA |

## Recovered behaviour
The routine takes no arguments, stores zero into a single word-sized global
flag, and returns nothing. It is the clearing half of a set/clear pair whose
setting half is the immediately preceding routine.

## Status
Byte-exact in USA. Already recovered before this assignment: the accepted unit
`func_80011918` (source `src/functions/func_80011918.c`) contains this function
alongside `func_80011918`, and the committed USA matching configuration already
lists it. Re-verified byte-exact here for USA, Japan and Europe against that
existing source. No new source file was written, because a second definition
would collide at link time.
