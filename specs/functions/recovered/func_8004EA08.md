# func_8004EA08

## Boundary and interface

USA ROM `0x4F608` / VRAM `0x8004EA08`, through VRAM `0x8004F250`:
`0x84C` bytes. The return at `0x8004F24C` executes the stack restore in its
delay slot. The next function starts at `0x8004F254`. The sole recovered direct
caller is `func_8004DC0C`, which passes its session pointer while handling the
`Load` command.

## Calls and global data

The routine calls `func_80028070`, `func_80039828`, `func_80046974`,
`func_80015268`, `func_80050208`, `func_80082BE0`, `func_80051934`, and
`func_8008A6B4`. It resets the control record at `D_8011A240`, four parallel
151-word arrays beginning at `D_8011A2A8`, and twelve 136-byte records at
`D_80118F90`. The eight-entry count template is `D_800A5228`; the eight
92-byte group descriptors start at `D_800D6DD8`; their fifteen total generated
items are 24-byte records at `D_80119670`.

## Recovered behavior

Reset the shared control arrays and helper subsystems, then give the supplied
session its default flags, vector, selectors, timing limits, and three initial
slot ordinals. Clear every shared event record. Partition fifteen item records
among eight groups according to the fixed count template, representing an
empty group with the range `-1..-2`.

For each group, draw randomized angular and rate parameters. Groups other than
the seventh avoid two angular bands by adding 90 degrees, while the seventh
gets a fivefold rate. Distribute each group's items around a full circle and
give each independently randomized position, velocity, and scale components;
one sign draw is deliberately shared by an item's second and third components.
Finally write the eight course display strings into the descriptors and clear
the completion flag at `D_800A4BE8`.

## Regional evidence

Japan enters the equivalent body at `0x8004F674`; Europe at `0x80053528`.
Their corresponding entry relocation supports the USA boundary and subsystem
identity. They are structural evidence only; this source is currently claimed
for USA, the canonical target.

## Status

Behavior-recovered C. With IDO 5.3 `-O2 -mips2` and the source object's
loop-unrolling setting, the compiled body is 2,128 bytes versus the original
2,124 and differs from the prologue. It is not eligible for substitution and
no byte match is claimed.
