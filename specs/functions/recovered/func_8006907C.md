# func_8006907C

## Verified facts

- The USA body spans VRAM `0x8006907C` through `0x800695D3`, ROM `0x69C7C`,
  1368 bytes. `jr ra` is at `0x800695CC`; its delay slot restores the
  136-byte stack frame. `func_800695D4` begins next.
- Its direct caller is `func_8006B568`. Its motion, vector, collision-query,
  and damping helpers and globals are recorded in the matching unit.
- Japan and Europe equivalents begin at `0x80069BEC` and `0x8006E55C`.

## Behavior

The routine combines the object's driving motion and transient impulses,
damps the stored impulses, and adjusts the velocity against its current contact
direction. It integrates the position for the frame, performs up to six
collision-resolution passes when enabled, and stores the resulting displacement
for later movement processing.

## Status

Behavior recovered from the USA binary and independently expressed in C; not
byte-matching. The target's square-root instruction confirms the IDO intrinsic
used by the source. IDO 5.3 `-O2` emits 1360 bytes for the 1368-byte target and
first differs at byte `0x3`; 340 instruction words differ across three blocks.
