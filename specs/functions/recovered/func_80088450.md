# func_80088450

## Boundary
VRAM 0x80088450-0x80088488 (56 bytes), ROM 0x89050. Ends with the return
instruction and an empty delay slot; two direct callers target the entry.
Boundary confirmed as assigned.

## Callers / callees
Callers: 2. Callees: func_80088020, func_80088050.

## Globals
None.

## Regional addresses
- Japan: 0x80089140, ROM 0x89D40, 56 bytes, verified exact
  (helpers at 0x80088D10 and 0x80088D40).
- Europe: 0x8008D9A0, ROM 0x8E5A0, 56 bytes, verified exact
  (helpers at 0x8008D570 and 0x8008D5A0).
- lrg_rev1: same address as USA, verified exact.

## Recovered behavior
Takes two pointers. First passes the second pointer alone to a preparation
helper; both incoming pointers are kept alive across that call. Then calls a
second helper with the same second pointer and, as its second argument, the
address 0x14 bytes into the first pointer's object. Returns nothing.

## Status
exact in USA, Japan, Europe and lrg_rev1.
