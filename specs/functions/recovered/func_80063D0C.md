# func_80063D0C

## Verified facts

- The canonical USA body is VRAM `0x80063D0C` through `0x80063E07`, ROM
  `0x6490C`, 252 bytes. The next function begins at `0x80063E08`.
- Matching entries zero through three return at `0x80063D4C`, `0x80063D80`,
  `0x80063DB4`, and `0x80063DE8`; their delay slots form the returned index.
  The no-match path returns at `0x80063E00` with a `nop` delay slot.
- Labels at `0x80063D54`, `0x80063D88`, `0x80063DBC`, and `0x80063DF0` are
  internal branch targets, not independent functions.
- The three direct callers are at `0x80062A40`, `0x80062AC8`, and
  `0x80063E58`. The function has no callees and reads only `D_800A5100`.
- Japan (`0x8006487C`, ROM `0x6547C`) and Europe (`0x800691EC`, ROM
  `0x69DEC`) have the same normalized structure across all 252 bytes.

## Behavior

The function searches sixteen three-float table entries for one whose first
two components exactly equal the two floats at object offsets `0x50` and
`0x54`. It checks entries in ascending order and returns the first matching
index. The third table component is ignored. If no pair matches, it returns
negative one. Unordered floating-point comparisons do not match.

## Status

Byte-matching C for canonical USA. The independently written four-at-a-time
loop compiles with IDO 5.3 `-O2` to all 252 original bytes exactly. The
canonical round trip reproduced SHA-1
`3542d5597c8a56ea8f5c63bceae97a24c4c08d58`.
