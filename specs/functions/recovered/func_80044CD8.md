# func_80044CD8 — matching blocker

- USA ROM `0x458D8..0x45C03` is an 812-byte body. The current C candidate has
  the correct linked symbol size and address, with 36 differing instruction
  words, first at byte `0x5`.
- Two declaration-order/padding variants increased the mismatch to 74–75
  words and were discarded. The residual differences begin in the compiler
  prologue and involve register scheduling and local-stack layout. Keep the
  USA split as assembly.
