# func_800845A0

## Verified facts

- The USA body spans VRAM `0x800845A0` through `0x80084A3B`, ROM `0x851A0`,
  1180 bytes. `jr ra` is at `0x80084A34`; its delay slot restores the 272-byte
  frame, and `func_80084A3C` begins next.
- `func_80084A3C` calls it, and the body recursively calls itself four times.
  Other callees are `func_800156DC`, `func_80015724`, `func_80017DA4`,
  `func_80017DAC`, `func_800182FC`, `func_80084324`, and `func_800844C8`.
- It uses traversal globals `D_800A6894`, `D_800A6898`, and `D_80120DA0`.
  JP and EU equivalents begin at `0x80085280` and `0x80089A80`.

## Behavior

The routine recursively traverses a typed scene-node hierarchy. Transform
nodes compose a local matrix and visit their children, selector nodes visit one
or all selected children, and eligible leaf nodes pass the accumulated matrix
and query arguments to the leaf evaluator while maintaining traversal state.

## Status

Behavior recovered, not byte-matching. IDO 5.3 `-O2` emits 1188 bytes against
the 1180-byte target and first differs at byte `0x3B`. The differences are
broad enough that the USA build retains assembly.
