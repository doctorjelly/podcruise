# func_80009C0C — matching blocker

- USA ROM `0xA80C..0xAB6B` is an 864-byte body. The current C candidate has
  the correct linked symbol size and address, with 37 differing instruction
  words, first at byte `0x169`.
- Two loop-shape forms and one declaration-order form did not improve the
  comparison. Differences begin at a late cue reload and appear to involve
  floating-point register allocation or scheduling. Keep assembly in USA.
