# func_8005EF08

## Boundary and evidence

USA is VRAM `0x8005EF08`, ROM `0x05FB08`, 1,044 bytes through
`0x8005F31C`. The return at `0x8005F314` has a `nop` delay slot. Its direct
caller is `func_80060880`. The unique equivalents are Japan `0x8005FA70` /
ROM `0x060670` and Europe `0x800643EC` / ROM `0x064FEC`.

## Behavior

Clear and initialize the shared resource slots, load the primary player
resource, record timing, and run the subsystem-specific setup stages. Select
and configure optional mode-dependent objects, populate six object slots and
two auxiliary objects, initialize two shared records, then choose display style
and flags from the current mode before applying the final renderer settings.

## Status

Behavior recovered only; the current compiler probe does not match the USA
function byte-for-byte.
