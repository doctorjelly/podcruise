# `func_8002CC28` USA compiler audit

The verified stitched USA entry begins at `8002CC28`; the three instructions
before its apparent `8002CC34` prologue belong to this function. Its current C
source compiles to the exact 1056-byte size but differs in 69 linked words,
starting at byte `0x00`.

At `8002CC54–8002CC94`, the ROM keeps the signed animation step in `$f0`,
the base step in `$f18`, and accumulated alpha in `$f2`. The current linked C
reuses `$f0` for the step and alpha, resetting it later at `8002CCC4`. Three
bounded source variants separated the step and alpha lifetimes: separate
`step`/`alpha` locals, reversed declaration order, and separate `step` with
the existing `value` alpha local. Each retained the 1056-byte size but
increased linked differences to 73 words. None was exact. All edits were
reverted, and the parent reran the 1056/1056, 69-word baseline verifier.

The ROM also accesses the alpha globals through `$at` while current C keeps
separate pointers in `$v1`/`$v0`. Do not repeat the local-split variants
without new register-lifetime evidence.
