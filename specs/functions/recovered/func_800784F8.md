# `func_800784F8`

## Boundary and interface

- Canonical USA interval: VRAM `[0x800784F8, 0x80079714)`, ROM
  `[0x790F8, 0x7A314)`, 4,636 bytes.
- The body creates a `0x1A8`-byte frame. Its return is `jr $ra` at
  `0x8007970C`; the executed delay slot at `0x80079710` is `nop`.
- One large craft/object pointer arrives in `a0`. The only recovered caller,
  `func_8007ACEC`, ignores the result, and the body establishes no return
  value, so the recovered interface is `void func_800784F8(void *craft)`.
- The unique regional equivalents are JP `func_80079128` and EU
  `func_8007D9D8`. They support the function boundary and broad control-flow
  structure; USA remains the source target.

## Direct evidence

The routine calls `func_8000E8C4`, `func_8000E9BC`, `func_80015268`,
`func_80015288`, `func_80015328`, `func_800156DC`, `func_80016BF4`,
`func_80017520`, `func_80017BA8`, `func_800181BC`, `func_8003E0A0`,
`func_800738D4`, `func_80075FC4`, `func_80077C94`, and `func_800834F0`.
It reads type-indexed values from `D_800A5CA0` and `D_800A5D00`, float
constants `D_800ADAA0` through `D_800ADAC0`, and double `D_80120BF0`.

The object contains an attached-resource pointer at `+0x344`, a type record
reached through `+0x1E70`, flags at `+0x60/+0x64`, a timer at `+0x1998`, and
many 4-by-4 transforms beginning at `+0x350`. The attached record supplies
nodes at `+0x28` through `+0x34` and exhaust-related nodes at `+0x10C`
through `+0x118`.

## Behavioral specification

Return immediately when the craft has no attached resource. Otherwise enable
its optional effect node and inspect the craft type and paired-attachment
state. For ordinary craft types, derive endpoints from four existing
transforms plus type-specific offsets and update the two connector effects.

Clamp the craft's heat and longitudinal-speed inputs. Build and publish two
exhaust transforms, then build two scale matrices whose length varies with
heat. Derive two exhaust colors and intensities from heat, signed speed, three
activity channels, channel flags, and a disabling craft flag. Clamp the color
components to 0--255 and intensities to 1--254, apply them to the two located
render resources, and advance their vertical texture scroll.

Update two additional craft effects. Before timer 76, construct up to four
attachment transforms between the craft's central and corner matrices and
publish them to their nodes; otherwise disable the first pair. During the
first 51 timer ticks, once per craft, initialize the first pair's deformable
meshes with speed-dependent scales.

## Recovery status

Behavior-recovered C. The source is independently expressed from the verified
USA instructions and local helper contracts. It is not byte-matching unless
the exact compiler comparison reports an identical 4,636-byte body.
