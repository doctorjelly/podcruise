# func_80006F60

## Boundary
VRAM 0x80006F60-0x80006FD3, 116 bytes. Ends with a return whose delay slot is
empty. Frame 0x48; the single caller targets the entry directly. Confirmed the
next label begins a separate body.

## Callers / callees
One direct caller. Calls func_80087CC0, func_80087D70, func_80087E80 in that
order.

## Globals
D_800D9BF8 — used only by address (never loaded through), passed to two of the
callees, so it is an object/array rather than a scalar.

## Regional addresses
- jp 0x80006F60 (rom 0x7B60), symbols remapped: func_800889B0, func_80088A60,
  func_80088B70, D_800DCE68.
- eu 0x80006F60 (rom 0x7B60), symbols remapped: func_8008D210, func_8008D2C0,
  func_8008D3D0, D_800E2898.
- lrg_rev1 shares the USA addresses exactly.

## Behavior
Takes three word-sized parameters. First it notifies a helper with the second
and third parameters. It then builds a request in a 24-byte area of its own
frame: the helper is given that scratch area, two zero selectors, the three
incoming parameters, and the address of the shared object D_800D9BF8. Finally it
asks a third helper to act on D_800D9BF8, handing it a one-word result slot in
the frame and the selector 1. Nothing is returned; the result slot is written by
the callee and not inspected here.

## Status
exact (USA, JP, EU and lrg_rev1 all byte-identical).
