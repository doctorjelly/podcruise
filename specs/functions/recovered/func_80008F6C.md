# func_80008F6C

## Verified facts

- The canonical USA function spans VRAM `0x80008F6C` through `0x80009133`,
  ROM `0x9B6C`, 456 bytes. Its final return is at `0x8000912C` with a `nop`
  delay slot; `func_80009134` begins immediately afterward.
- The apparent functions from `0x80008F80` through `0x80009118` are switch
  entries and shared control-flow labels. No call targets them. Six direct
  calls target `func_80008F6C`, from `func_800092EC` and
  `func_800093B0`.
- If the detail argument is negative one, the entry returns negative one at
  `0x80008F78`; that return's delay slot loads the result. Otherwise its
  branch-likely delay slot performs the switch range check before entering the
  shared dispatcher.
- The function has no callees. Groups zero through seven read signed
  halfwords from `D_8009A6F0`, `D_8009A758`, `D_8009A7A4`, `D_8009A818`,
  `D_8009A824`, `D_8009A8F4`, `D_8009AA48`, and `D_8009AB1C`. The jump table
  at `0x800A8200` is compiler-generated read-only data.
- Japan and Europe retain the same dispatcher layout and address-based entry
  labels. Their relocated data references differ, so no cross-region byte
  match is claimed.

## Behavior

The function validates a three-part resource identifier. Groups outside zero
through seven fail. Groups zero and one additionally require the owner to be
between zero and 22. Every group requires a positive detail index below its
table-specific limit: 51, 38, 57, 5, 104, 169, 105, or 168 respectively.
Failures return negative one. Success reads the group's signed halfword and
packs it with the group in bits 24 through 31, the owner in bits 16 through
23, and flag bit `0x8000`.

## Status

Byte-matching C for canonical USA. The independently written switch compiles
with IDO 5.3 `-O2` to all 456 original bytes exactly. Regional equivalence is
structural evidence only. The integrated canonical round trip reproduced
SHA-1 `3542d5597c8a56ea8f5c63bceae97a24c4c08d58`.
