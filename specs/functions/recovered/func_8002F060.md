# func_8002F060

## Boundary

- USA VRAM range 0x8002F060 .. 0x8002F06B inclusive, ROM 0x2FC60, 12 bytes
  (3 instructions). The assignment size of 12 is correct; no correction needed.
- The body is an address-high computation, the return, and the load of the
  value in the return delay slot. No stack frame.

## Callers and callees

- Fifteen direct call sites in the USA build. Callees: none (leaf).

## Globals and access widths

- `D_800D7740` - single-precision floating point value, read with a 32-bit
  float load. Read only.

## Arguments and result

No arguments. Result is the floating point value.

## Recovered behavior

A plain accessor: it returns the current value of one global single-precision
setting.

## Status

skipped - already recovered by another worker before this assignment was
picked up. The function is defined in `src/functions/func_8002F054.c` (a unit
that covers both `func_8002F054` and `func_8002F060`) and that unit is already
registered in `config/c_matching.json`. Re-verified byte identical at
0x8002F060 / ROM 0x2FC60 / 12 bytes against that existing source. No duplicate
definition was added; the file this worker had drafted was deleted so the host
link keeps a single definition.
