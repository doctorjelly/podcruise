# func_8003D444

## Boundary
VRAM 0x8003D444-0x8003D488, ROM 0x3E044, size 68 bytes. Ends with the return
instruction and an empty delay slot. One direct caller targets the entry.

## Callers / callees
1 caller. Calls func_8003D110 then func_8003B300.

## Globals
D_80114470: a pair of signed 16-bit values at offsets 0 and 2.

## Regional addresses
jp 0x8003DDC4 (ROM 0x3E9C4), eu 0x80041D84 (ROM 0x42984), lrg_rev1 shares the
USA address. Regional bodies are structurally identical with relocated symbols
(jp: func_8003DA90, func_8003BC80, D_801176E0; eu: func_80041A50,
func_8003FC40, D_8011D110).

## Behavior
Takes no arguments and returns nothing. First performs the preparation step
implemented by func_8003D110. Then reads the two signed 16-bit fields of the
global pair, and calls func_8003B300 with a rectangle expressed as four
values: zero, the first field less one, zero, and the second field less one.
That is, it submits the inclusive full extent described by the global size
pair.

## Status
exact (USA, JP, EU, lrg_rev1 all byte-identical).
