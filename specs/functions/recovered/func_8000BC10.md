# func_8000BC10

## Verified facts

- The USA body spans VRAM `0x8000BC10` through `0x8000C52F`, ROM `0xC810`,
  2336 bytes. `jr ra` is at `0x8000C528`; its delay slot restores the
  328-byte stack frame. `func_8000C530` begins next.
- The direct caller is `func_8002F750`. The body uses the transform, vector,
  object, and scene helpers and globals listed in its matching unit.
- The Japan and Europe structural equivalents both begin at `0x8000BC10`.

## Behavior

The routine updates a table of 32 scene-node transforms. It marks nodes used
by active objects, copies or constructs their matrices, then applies configured
offsets, rotations, or look-at transforms. Finally it writes the resulting
transforms back to the associated objects and shared scene state.

## Status

Behavior recovered from the USA binary and independently expressed in C; not
byte-matching. IDO 5.3 `-O2` emits the exact 2336-byte body and first differs at
byte `0x475`; 97 instruction words differ across 39 blocks. Regional binaries
were used only to confirm structure and relocation.
