# func_8008C638

## Verified facts

- The canonical USA body spans VRAM `0x8008C638` through `0x8008C6D3`, ROM
  `0x8D238`, 156 bytes. Its return is at `0x8008C6CC`, with the o32 64-bit
  result adjustment in the delay slot. `func_8008C6D4` begins next.
- The function has no direct callers or callees. It temporarily changes FCSR,
  uses MIPS III 64-bit conversion instructions, restores FCSR, and reads the
  unsigned high-bit constant at `0x800ADE78` on the large-value path.
- The structurally equivalent leaves begin at Japan VRAM `0x8008D328`, ROM
  `0x8DF28`, and Europe VRAM `0x80091B88`, ROM `0x92788`.

## Behavior

The function converts its single-precision argument to an unsigned 64-bit
integer. Values at or above the signed 64-bit boundary are converted after
subtracting that boundary and then have the unsigned high bit restored.
Invalid or out-of-range conversions produce the runtime's all-ones sentinel.

## Status

Behavior recovered and byte-matching. The independent C cast compiles with
IDO 5.3 `-O1 -mips3 -32` to the exact 156 USA bytes, including the FCSR hazard
nops, exception-bit checks, unsigned high-bit path, and the return delay slot.
The same compiler comparison also matches LRG rev1 exactly. Japan and Europe
remain structural evidence only.
