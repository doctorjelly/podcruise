# `func_8007E988` recovery note

Correct-size USA C remains non-matching: the baseline and three
declaration/layout variants all compile to 500 bytes but differ in 47 words
(first difference `0x4C`). Removing the scratch local, swapping matrix
declaration order, and moving the scratch local after the matrices did not
yield an exact match. The remaining mismatch is broad register
allocation/scheduling plus local stack placement; no source variant was
retained.
