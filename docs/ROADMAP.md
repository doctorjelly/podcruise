# Decompilation roadmap

## Definition of done

“100%” means that all four unique supplied images can be rebuilt byte-for-byte
from reviewed shared/version-specific source, data declarations, assets
extracted from user-supplied ROMs, and a reproducible toolchain. USA retail is
the canonical source-matching baseline. A mechanically recompiled executable or
a list of discovered functions does not count as source-decompilation progress.

Progress will eventually be reported separately for:

- matching code bytes;
- identified functions and symbols;
- typed data and reconstructed assets;
- regional build coverage;
- runtime behavior tests.

## Version policy

| Version | Purpose |
|---|---|
| USA retail (`NEPE`, revision 0) | canonical matching target |
| Japan retail (`NEPJ`, revision 0) | later-build code and boundary evidence |
| Europe retail (`NEPP`, revision 0) | PAL/localization code and boundary evidence |
| USA Limited Run Games (`NEPE`, revision 1) | later comparison/build target; three supplied filenames are identical |

Japan and Europe are not automatically “newer and better” source. Differences
must be classified as relocation, compiler/code change, localization, timing,
asset, or post-link patch before being used.

## Phase 0: reproducible evidence

- [x] Hash and validate all supplied ROMs.
- [x] Detect duplicate filenames/images.
- [x] Establish a conservative USA executable interval.
- [x] Derive independent function candidates.
- [x] Map normalized candidate windows into Japan and Europe.
- [ ] Confirm executable/data boundaries with runtime DMA tables and splat.
- [x] Establish an IDO-family compiler hypothesis from control-flow lowering.
- [x] Identify IDO 5.3 across 439 exactly reproduced functions, including the
      VR4300 multiply workaround `-Wab,-r4300_mul`.
- [x] Determine whether other translation units use different compiler versions
      or optimization flags. Matching units may carry per-unit `flags`; `-O1`,
      `-O3`, loop-unrolling-disabled and MIPS III objects are all confirmed.
- [ ] Map the boundaries of those differently built translation units.
- [ ] Identify exact libultra/SDK variants by instruction fingerprints.

## Phase 1: matching build loop

- [x] Pin the Python splitter/disassembler environment by exact version.
- [x] Pin the local GNU MIPS binutils package and IDO static-recomp release by
      cryptographic hash; keep both outside Git.
- [x] Identify the local-only IDO 5.3 matching profile for the first tranche.
- [ ] Split USA into boot, code, data, and asset segments.
- [x] Reassemble the coarse untouched split to an exact USA ROM.
- [x] Substitute 1,041 matching C functions while retaining the exact USA ROM.
- [x] Substitute 1,012 verified matching functions in the exact LRG revision.
- [ ] Refine USA into verified boot, CPU code, data, RSP microcode, and asset
      segments while preserving the exact round trip.
- [ ] Add `make verify` with whole-ROM SHA-1 and first-difference diagnostics.
- [ ] Configure `asm-differ` for function-level iteration.

## Phase 2: source recovery

- [x] Replace the hand-maintained inventory with `analysis/source_manifest.json`,
      generated by `make manifest` from the matching configuration and the
      per-version reports. 1,348 functions have reviewed C; 1,041 match USA
      exactly.
      The target fails if any recovered source is left unmeasured.
- [ ] Name and type libultra functions first.
- [x] Recover leaf utilities and string/memory routines.
- [x] Identify and label the units that cannot be C, by instruction class
      rather than by a disassembler heuristic. `make classify` writes
      `analysis/non_c_functions.json`; the compiler-capability facts it rests
      on are pinned by tests.
- [ ] Reconstruct scheduler, DMA, controller, audio, and graphics boundaries.
- [ ] Decompile game systems in dependency order.
- [ ] Require byte match plus a short evidence note for every source conversion.

## Phase 3: regional builds

- [x] Partition common source: 397 functions are independently verified in
      all four builds from one shared source tree.
- [x] Build Japan (399 functions) and Europe (397) from the shared C tree.
- [ ] Reconstruct localization and PAL timing changes explicitly.
- [x] Add the one unique LRG image as an exact hybrid build; validate its three
      local filename aliases through the inventory.
- [ ] Recover and classify the code/data differences unique to each build.

## Evidence rules

1. ROM-derived generated files stay out of Git unless they are clearly lawful,
   compact metadata such as hashes and addresses.
2. A structural cross-version match supports a hypothesis; it does not prove a
   function boundary or semantic equivalence.
3. Imported names or addresses must have a provenance note.
4. Prior-art code with no explicit license is not copied.
5. Every claimed matching function is verified against each ROM for which a
   byte match is reported.
